__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.spec
*.log

# Local runtime state — copy devices.example.json to devices.json locally
devices.json

# Device config backups (may contain secrets)
config/*/
!config/.gitkeep

# Keep release archives, ignore accidental local junk inside them
releases/**/devices.json
